TAB	EQU	30H
	LJMP  START
	ORG  100H
START:
	MOV  TAB,#0
	MOV  TAB+1,#2
	MOV  TAB+2,#4
    MOV  TAB+3,#6
	MOV  TAB+4,#8
    MOV  TAB+5,#10h
 	MOV  R0,#TAB
 	CALL  LCD_CLR
ABCD:
 	MOV  A,@R0
 	CALL  WRITE_HEX
 	MOV  A,#5
 	CALL  DELAY_100MS
 	CALL  LCD_CLR
 	INC  R0
 	CJNE  R0,#TAB+6,ABCD
	MOV  R0,#TAB
 	SJMP  ABCD
